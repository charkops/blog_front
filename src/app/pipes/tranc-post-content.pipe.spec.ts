import { TrancPostContentPipe } from './tranc-post-content.pipe';

describe('TrancPostContentPipe', () => {
  it('create an instance', () => {
    const pipe = new TrancPostContentPipe();
    expect(pipe).toBeTruthy();
  });
});
